<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Material">
        <!-- Customize your theme here. -->
    </style>

    <style name="SignName" parent="TextAppearance.AppCompat.Headline">
        <item name="android:gravity">center</item>
    </style>

</resources>
